overwrite: true
watch: true
schema: ../embed/schema.graphql
documents: src/**/*.graphql
generates:
  src/graphql.g.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      skipTypeNameForRoot: true
      skipTypename: true
