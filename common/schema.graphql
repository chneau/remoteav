schema {
  query: Query
  mutation: Mutation
}

type Query {
  cameras: [Camera!]!
}

type Mutation {
  setCamera(id: Int!, format: String!, frameSize: String!): Boolean!
}

type Camera {
  id: Int!
  supportedFormats: [SupportedFormat!]!
}

type SupportedFormat {
  format: String!
  frameSizes: [String!]!
}
