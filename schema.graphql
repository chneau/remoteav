schema {
  query: Query
}

type Query {
  cameras: [Camera!]!
}

interface SupportedFormat {
  format: String!
  frameSizes: [String!]!
}

interface Camera {
  id: Int!
  supportedFormats: [SupportedFormat!]!
}
