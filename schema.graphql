schema {
  query: Query
}

type Query {
  cameras: [Camera!]!
}

interface Camera {
  id: Int!
  supportedFormats: [SupportedFormat!]!
}

interface SupportedFormat {
  format: String!
  frameSizes: [String!]!
}
