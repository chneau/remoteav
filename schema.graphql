schema {
  query: Query
}

type Query {
  cameras: [Camera!]!
}

type Camera {
  id: Int!
  supportedFormats: [SupportedFormat!]!
}

type SupportedFormat {
  format: String!
  frameSizes: [String!]!
}
